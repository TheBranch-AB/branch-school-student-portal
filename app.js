function tick(){const n=new Date();clock.textContent=new Intl.DateTimeFormat(undefined,{hour:'numeric',minute:'2-digit'}).format(n);date.textContent=new Intl.DateTimeFormat(undefined,{weekday:'long',month:'long',day:'numeric'}).format(n)}tick();setInterval(tick,30000);signin.addEventListener('click',()=>alert('Next step: add one Google OAuth Web Client ID and connect Calendar API.'));